---
all:
  children:
    android_farm_hosts:
      hosts:
        farm-01:
          ansible_host: 203.0.113.10
          ansible_user: ubuntu
          # Store this value with ansible-vault; never commit the plaintext.
          farm_redis_worker_password: "{{ vault_farm_redis_worker_password }}"
